# Obter token de autenticacao

Le o token JWT armazenado na variavel de ambiente `FICHARIUM_TOKEN`.

## Usage

``` r
ficharium_token()
```

## Value

string com o token

## Details

Obter token de autenticacao

## Examples

``` r
if (FALSE) { # \dontrun{
ficharium_token()
} # }
```
